# delete ambiente
# criar ambiente
# realizar deployments
# rodar K6 (2m primeiro, 5m depois)
# rodar script "app.py" para gerar dados brutos
# rodar sh (via bash) "generateLogs.sh" para  gerar logs de microcontroladores (usar parametro igual ao script Python 3m e 6m)
# acompanhar geração de logs
# Copiar logs para a pasta "Summary"
# Rodar script "run.sh" para gerar summary
# Criar uma pasta nova para o script